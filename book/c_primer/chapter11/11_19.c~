#include <stdio.h>
#include <string.h>
#define SIZE 81
#define LIM 100
#define STOP "quit"
int main(void){
  char input[LIM][SIZE];
  int ct=0;
  
  printf("enter:\n");
  while(ct<LIM&&gets(input[ct]!=NULL)&&strcmp(inputp[ct],STOP)!=0){
    ct++;
  }
  printf("%d string entered\n",ct);
  return(0);
}
